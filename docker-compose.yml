version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flasycardycourse-web
    env_file:
      - .env
    ports:
      - "8080:80"  # host:container
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    depends_on:
      - db

  db:
    image: postgres:18
    container_name: flasycardycourse-db
    restart: unless-stopped
    environment:
      - POSTGRES_DB=flasycardycourse
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - PGDATA=/var/lib/postgresql/18/docker
    ports:
      - "8032:5432"
    volumes:
      - ./.volumes/postgresql:/var/lib/postgresql

  # pgadmin:
  #   image: dpage/pgadmin4
  #   container_name: flasycardycourse-pgadmin
  #   restart: unless-stopped
  #   environment:
  #     - PGADMIN_DEFAULT_EMAIL=admin@example.com
  #     - PGADMIN_DEFAULT_PASSWORD=admin
  #   ports:
  #     - "8033:80"
  #   depends_on:
  #     - db